# Machotkey documentation artifacts

This directory holds **published** reference material for the project, **not** general legal prose.

| Path | Purpose |
|------|---------|
| **`web/docs/`** | Static **API reference** (`index.html` and linked assets). Open locally in a browser or serve via your preferred static host. |
| **`web/guide/`** | Static **guide** pages (`index.html`). |
| **`manifest.schema.json`** | JSON Schema for macro **`manifest.json`** validation / editor tooling. |

**Licensing for users and readers** is explained at the repository root: **[LEGAL.md](../LEGAL.md)** (guide) and **[LICENSE](../LICENSE)** (legal text).